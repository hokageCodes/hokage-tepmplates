import Button from "../ui/Button";

export default function FinalCTA() {
  return (
    <section className="w-full text-center py-12 bg-accent/5 rounded-2xl border border-border">
      <h2 className="text-2xl font-extrabold mb-6 text-neutral-900">Just need a clean place to start?</h2>
      <Button href="#" size="md" variant="accent">Get the templates</Button>
    </section>
  );
}
